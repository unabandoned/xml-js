name: Renovate Config

# Validate this fork's renovate.json against Renovate's schema so a
# malformed config can't silently halt Renovate on a bot- or
# Claude-authored PR. Delegates to the shared reusable workflow.
on:
  push:
    branches: [master]
  pull_request:

jobs:
  renovate-config:
    permissions:
      contents: read
    uses: unabandoned/.github/.github/workflows/reusable-renovate-config.yml@b7d15f415eb7e6475523b40d929e8a7a0d90a82f # v1.0.3
